다음 순서로 배포를 진행해줘:

1. **테스트 실행**: `python scraper.py "강남구 테헤란로 152"` 를 실행하고 에러 없이 JSON이 반환되는지 확인
2. **검증 통과 시**: `git add -A` → `git commit` → `git push` 순서로 배포
3. **검증 실패 시**: 배포를 중단하고 오류 내용을 보고

커밋 메시지는 변경 내용을 한국어로 간결하게 요약해줘 (예: `토지이용계획 스크래퍼 추가`)
